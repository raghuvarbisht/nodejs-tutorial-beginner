const sum = (a,b) => {
    console.log(`sum of ${a} and ${b} is ${a+b}`);
}
const subtarction = (c,d) => {
    console.log(`subtarction of  ${c} and ${d} is ${c - d}`);
}

module.exports = {
  sum,
  subtarction
};

